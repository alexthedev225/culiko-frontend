/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "imgs.search.brave.com",
      "media.istockphoto.com",
    ],
  },
};

export default nextConfig;
